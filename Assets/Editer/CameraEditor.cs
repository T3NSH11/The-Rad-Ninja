using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using UnityEditor;

//[CustomEditor(typeof(CameraController))]
/*public class CameraEditor : Editor
{
    /*
    SerializedProperty mouseSensitivity;

    SerializedProperty maxYRotation;
    SerializedProperty minYRotation;

     




    private void OnEnable()
    {
        mouseSensitivity = serializedObject.FindProperty("mouseSensitivity");

        minYRotation = serializedObject.FindProperty("minYRotation");
        maxYRotation = serializedObject.FindProperty("maxYRotation");
    }

    public override void OnInspectorGUI()
    {
        serializedObject.Update();

        DrawHeader();
        //base.OnInspectorGUI();
        EditorGUILayout.PropertyField(mouseSensitivity);
        EditorGUILayout.LabelField("Some text.");
        EditorGUILayout.Space();
        //EditorGUILayout.PropertyField(minYRotation);
        EditorGUILayout.Vector2Field("Min and Max Movement on Y-axis", new Vector2(minYRotation.floatValue, maxYRotation.floatValue));
        EditorGUILayout.LabelField("Determines the max amount the camera can move up and down.");

        serializedObject.ApplyModifiedProperties();
    }
    
}
*/
